// Third party dependencies
import styled from 'styled-components';

const AtomTableIconWrapper = styled.div`
        display: inline;
        height: 21px;
        position: relative;
        top: 4px;
        margin-right: 7px;

        & svg{
            height: 21px;
        }
`;

AtomTableIconWrapper.displayName = 'AtomTableIconWrapper';

export default AtomTableIconWrapper;