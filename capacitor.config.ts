import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'im.oriol.passager',
  appName: 'Passager Password Manager',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
